#include <stdio.h>
#include "main.h"

/**
 * main - entry pint
 *
 * @argc: the count
 * @argv: the array
 * Return: 0 success
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	(void) argc;
	return (0);
}
